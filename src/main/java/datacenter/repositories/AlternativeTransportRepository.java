package datacenter.repositories;

import datacenter.models.AlternativeTransport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AlternativeTransportRepository extends JpaRepository<AlternativeTransport, Long> {
}
