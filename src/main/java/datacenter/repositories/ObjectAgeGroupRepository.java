package datacenter.repositories;

import datacenter.models.ObjectAgeGroup;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ObjectAgeGroupRepository extends JpaRepository<ObjectAgeGroup, Long> {
}
